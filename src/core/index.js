/**
 * Core module - Main entry point
 * @module core
 */
const utils = require('../utils');
const services = require('../services');

module.exports = {
    version: '1.0.0',
    utils,
    services
};
